# Normalize line endings: LF in the repo and on checkout (Go/Python/sh tooling
# expects LF; prevents CRLF noise when contributing from Windows).
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
